{
  "name": "hub-integration-marketplaces",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "start:dev": "npm -w backend run start:dev & npm -w frontend run dev",
    "build": "npm -w backend run build && npm -w frontend run build"
  },
  "dependencies": {
    "@nestjs/common": "^11.1.12",
    "@nestjs/core": "^11.1.12",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/uuid": "^11.0.0"
  }
}
